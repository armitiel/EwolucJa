{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "GMAccount",
  "description": "Konto Game Mastera (rodzic lub nauczyciel). Dziecko widzi JEDNĄ wspólną personę GM, niezależnie od liczby kont dorosłych.",
  "type": "object",
  "required": ["account_id", "role", "name", "paired_player_ids"],
  "properties": {
    "account_id": { "type": "string" },
    "role": {
      "type": "string",
      "enum": ["parent", "teacher"]
    },
    "name": {
      "type": "string",
      "description": "Imię dorosłego (widoczne tylko w panelu GM, NIE dla dziecka)"
    },
    "email": { "type": "string", "format": "email" },
    "paired_player_ids": {
      "type": "array",
      "description": "Lista dzieci podpiętych do tego konta",
      "items": { "type": "string" }
    },
    "gm_persona_id": {
      "type": "string",
      "description": "ID wspólnej persony GM widzianej przez dziecko (klucz łączący różne konta dorosłych)"
    },
    "created_at": { "type": "string", "format": "date-time" }
  }
}
